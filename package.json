{
  "name": "nameweave",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Preview a batch rename, catch naming collisions, then download renamed copies in a ZIP. Originals stay untouched.",
  "license": "MIT",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "start": "node scripts/serve.mjs",
    "format": "prettier --write src scripts tests"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "prettier": "3.8.1"
  },
  "dependencies": {
    "fflate": "0.8.2"
  }
}
